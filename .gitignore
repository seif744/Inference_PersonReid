# YOLO model weights (auto-downloaded on first run; don't commit binaries)
*.pt

# Test / output artifacts
detection_test.jpg
*.mp4
*.mov
Screenshot 2026-07-17 181153.png
vlc-record-*.avi
!sample.mp4        # ...but DO allow a deliberately-added sample video if you add one

# Saved person crops (can grow large; regenerated by running the app)
crops/

# Runtime verifier decision log (regenerated by the file-batch verifier;
# see README section 7, "Understand the output"). Kept out of git so runs don't
# create diffs.
logs/

# Secrets -- API keys / server creds. NEVER commit.
.env

# Local Qdrant store (dev only; regenerated. Prod uses the Qdrant server).
qdrant_data/

# Python
__pycache__/
*.pyc
.venv/
venv/
qdrant_storage/
randperson_subset.tar.gz
randperson_subset/
prepared/randperson/
reports/
*.avi
models/

# Deploy target (SSH creds/path for deploy.sh) -- never commit
.deploy.env
# Acceptance/stage tests -- kept local. These drive the live pipeline with
# SYNTHETIC frames (tests/live/_synth.py), so they are dev harness rather than a
# shipped suite. Untracked with `git rm -r --cached tests` (files stay on disk).
tests/

# Local design/planning notes -- kept out of git by choice. NOTE: because these are
# untracked, git carries no history for them, so any revision record has to live
# inside the documents themselves (see ADR-003 section 7).
NVIDIA_DeepStream_Adoption_Plan.md
ADR-003-geometric-signal.md
ADR-003A-geometry-subsystem.md
ADR-003B-label-mining.md
ADR-003C-identity-integration.md

